<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Code Reserve | AddCode</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&display=swap" rel="stylesheet">
        <style>
            body {
                margin: 0;
                padding: 0;
                background-color: #f4f4f4;
                font-family: "Indie Flower", cursive;
                background-image: 
                linear-gradient(rgba(0, 0, 0, 0.02) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 0, 0, 0.02) 1px, transparent 1px);
                background-size: 20px 20px;
                overflow: hidden;
            }
            .container {
                height: 75%;
                width: 25%;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                position: absolute;
                background-color: #ffffff;
                border-radius: 10px;
                box-shadow: 0 8px 32px rgba(140, 80, 100, 0.2);
            }
            h1 {
                text-align: center;
            }
            form {
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
            }
            input {
                height: 2rem;
                width: 80%;
                border: 1px solid black;
                font-family: "Indie Flower", cursive;
                border-radius: 10px;
                margin-bottom: 12px;
                text-align: center;
            }
            input::placeholder {
                text-align: center;
            }
            textarea {
                height: 11rem;
                width: 80%;
                border: none;
                border-radius: 10px;
                margin-bottom: 12px;
                border: 1px solid black;
                overflow-x: auto;
            }
            textarea::-webkit-scrollbar {
                display: none;
            }
            button {
                font-family: "Indie Flower", cursive;
                padding: 5px;
                border: none;
                box-shadow: 0 8px 32px rgba(140, 80, 100, 0.2);
                border-radius: 8px;
                font-size: 1rem;
                font-weight: 600;
                color: white;
                background-color: black;
            }
            button:hover {
                cursor: pointer;
                background-color: #ffffff;
                color: black;
            }
            #back {
                top: -5%;
                left: 3%;
                position: absolute;
                cursor: pointer;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>Add Code</h1>
            <form action="/" method="post">
                <label for="title">Title</label>
                <input type="text" placeholder="Enter a descriptive title..." id="title" name="title">
                <label for="code">Code Block</label>
                <textarea name="codeBlock" id="code"></textarea>
                <button>+Add</button>
            </form>
            <h1 id="back">←</h1>
        </div>

        <script>
            let backBtn = document.querySelector("#back");
            backBtn.addEventListener('click', ()=>{
                window.location.href = "/";
            })
        </script>
    </body>
</html>